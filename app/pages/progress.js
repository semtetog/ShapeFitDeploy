// Página Progress
export const title = 'Progresso';

export default async function renderProgress(user, params) {
    return `
        <div class="progress-page">
            <h1 class="page-title">Progresso</h1>
            <p>Página de progresso em desenvolvimento...</p>
        </div>
    `;
}

export async function onLoad(user, params) {
    // Implementar lógica de progresso
}

