{
  "name": "shapefit-app",
  "version": "1.0.0",
  "description": "ShapeFit Mobile App",
  "main": "app/index.html",
  "scripts": {
    "build": "echo 'Build completo. Use: npx cap sync'",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios",
    "copy": "npx cap copy",
    "serve": "npx cap serve"
  },
  "dependencies": {
    "@capacitor/core": "^5.5.0",
    "@capacitor/app": "^5.0.0",
    "@capacitor/network": "^5.0.0",
    "@capacitor/splash-screen": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.5.0",
    "@capacitor/android": "^5.5.0",
    "@capacitor/ios": "^5.5.0"
  }
}

