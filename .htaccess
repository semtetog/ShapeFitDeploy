# Permite que o cabeçalho de autenticação personalizado seja passado para o PHP
RewriteEngine On
RewriteCond %{HTTP:X-Auth-Token} .
RewriteRule .* - [E=HTTP_X_AUTH_TOKEN:%{HTTP:X-Auth-Token}]

# Informa ao servidor como tratar arquivos .lottie
AddType application/zip .lottie